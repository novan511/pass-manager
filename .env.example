# Supabase Auth (login + reset password)
NEXT_PUBLIC_SUPABASE_URL=https://azvszagdpakwxaacaacd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — never expose to the browser / never commit the real value
SUPABASE_SERVICE_ROLE_KEY=

# App
SESSION_SECRET=
APP_URL=http://localhost:3000

# PostgreSQL (Supabase Database connection string — Settings → Database → Pooler URI)
# postgresql://postgres.xxxx:password@aws-0-xxx.pooler.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=
